mkdir -p ~/.streamlit/

echo "[server]
headless = true
port = \$PORT
enableCORS = false
" > ~/.streamlit/config.toml
" > ~/.streamlit/config.toml
" > ~/.streamlit/config.toml
headless = true\n\
enableCORS = false\n\
port = $PORT\n\
" > ~/.streamlit/config.toml
